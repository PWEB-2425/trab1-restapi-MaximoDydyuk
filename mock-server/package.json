{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Mock server para a API de alunos",
  "scripts": {
    "start": "json-server --watch ../mock-data/bd.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
